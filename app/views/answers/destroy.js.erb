const answer_<%= @answer.id %> = document.querySelector('.answer_<%= @answer.id %>');
if ( answer_<%= @answer.id %> ) {
  document.querySelector('.answers_count').innerHTML = "<strong> <%= @answer.question.answers.count %>&nbsp;Answers</strong>"
  answer_<%= @answer.id %>.remove();
}
